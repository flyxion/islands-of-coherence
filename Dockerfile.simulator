FROM python:3.12-slim

RUN pip install --no-cache-dir \
    numpy scipy matplotlib sympy pillow tqdm jupyterlab

WORKDIR /app
COPY src/python/requirements.txt .  # Optional, for clarity
CMD ["python", "run_all_simulations.py"]
